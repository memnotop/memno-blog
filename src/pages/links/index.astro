---
import { Content, frontmatter, getHeadings } from '../../data/pages/links.mdx'
import PageLayout from '@/layouts/CommonPage.astro'

const headings = getHeadings()
---

<PageLayout title={frontmatter.title} {headings} comment>
  <Content />
</PageLayout>
