---
import { Content, frontmatter } from '../data/pages/promise.mdx'
import PageLayout from '@/layouts/BaseLayout.astro'

const meta = {
  description: frontmatter.description,
  title: frontmatter.title
}
---

<PageLayout {meta} comment>
  <Content />
</PageLayout>
